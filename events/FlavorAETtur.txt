country_event = {
	id = flavor_AETtur.0001 #intended as an AI enhancement but also a boost to player expansion
	title = "flavor_AETtur.EVTNAME0001"
	desc = "flavor_AETtur.EVTDESC0001"
	picture = HORDE_ON_HORSEBACK_eventPicture

	trigger ={
		tag = TUR
		owns = 151
		religion = sunni
		
	}
	
	mean_time_to_happen = {
		days = 1
	}
	
	fire_only_once = yes
	
	option = {		# 
		name = "flavor_AETtur.EVTOPTA0001"
		ai_chance = { factor = 90 }
			dulkadir_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			aleppo_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			syria_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			palestine_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			bahari_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			delta_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			cyrenaica_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
			vostani_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = TUR
			}
		}
	}


	country_event = {
		id = flavor_AETtur.0002 #Used to slow down AI expansion and development lategame.
		title = "flavor_AETtur.EVTNAME0002"
		desc = "flavor_AETtur.EVTDESC0002"
		picture = SIEGE_eventPicture
		
		trigger = {
			tag = TUR 
			134 = {
				owned_by  = HAB} 
			is_year = 1700
			NOT = {war_with = HAB}
		}

		is_triggered_only = yes
		fire_only_once = yes
		mean_time_to_happen = {
			days = 1
		}
		option = {
			name = "flavor_AETtur.EVTOPA0002" 
			add_prestige = -20
			add_stability = -1
			add_country_modifier = {
				name = aet_ottoman_stagnation
				
			}

			}
				
			}

		country_event = {
			id = flavor_AETtur.0003 #How to end the Otoman stagnation.
			title = "flavor_AETtur.EVTNAME0003"
			desc = "flavor_AETtur.EVTDESC0003"
			picture = GOOD_WITH_MONARCH_eventPicture

			trigger = {
				prestige = 75
				legitimacy = 50
				average_autonomy = 25
				religious_unity = 100
				corruption = 0
				total_base_tax = 2000
			}
		}
	
